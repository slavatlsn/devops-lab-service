name: Auto Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Go to project
        run: |
          cd home/slava/devops-lab-service
          git pull
          sudo docker compose up -d --build
